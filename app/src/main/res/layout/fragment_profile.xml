<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light"
    tools:context=".app.ui.ProfileFragment">

    <LinearLayout
        android:orientation="vertical"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/img_avatar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/ic_user"
            android:background="@drawable/bg_rounded_md"
            android:padding="12dp"
            android:layout_marginBottom="16dp"
            android:tint="@color/orange_700" />

        <TextView
            android:id="@+id/tv_user_name"
            android:text="Lokesh Bhumia"
            style="@style/u_text_xl"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/tv_user_email"
            android:text="lokeshbhumia123@gmail.com"
            style="@style/u_text_gray"
            android:layout_marginBottom="24dp" />

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_rounded_md"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_balance"
                android:text="💰 Balance: ₹0.00"
                style="@style/u_text_lg"
                android:textColor="@color/orange_700"
                android:textStyle="bold"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tv_tasks_completed"
                android:text="✅ Tasks completed: 0"
                style="@style/u_text_gray"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tv_join_date"
                android:text="📅 Joined: --/--/----"
                style="@style/u_text_gray" />
        </LinearLayout>

        <Button
            android:id="@+id/btn_logout"
            style="@style/u_btn_orange"
            android:text="Logout"
            android:layout_marginTop="30dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>
</ScrollView>